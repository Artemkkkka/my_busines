volumes:
  pgdata:
services:
  database:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: default_user
      POSTGRES_PASSWORD: default_pass
      POSTGRES_DB: busines_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
