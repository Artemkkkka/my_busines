services:
  api-tests:
    build: .
    profiles: ["test"]
    environment:
      TESTING: "1"
      PYTHONDONTWRITEBYTECODE: "1"
      DATABASE_URL: ${TEST_DB_URL}
    volumes:
      - ./:/app
    command: >
      bash -lc "pip install -r requirements.txt && pytest"
